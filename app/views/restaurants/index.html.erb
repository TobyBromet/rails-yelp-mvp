<h1>Restaurants List</h1>

<p><%= link_to "Create new restaurant", new_restaurant_path, class: "btn btn-primary"%> </p>

<% @restaurants.each do |restaurant| %>
  <%= render "restaurants/display_list", restaurant: restaurant %>
  <p><%= link_to "See details", restaurant_path(restaurant.id), class: "btn btn-primary"%></p>
  <p> --------------------------------------------------- </p>
<% end %>
